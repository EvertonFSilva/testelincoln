services:
  - type: web
    name: cl-vidracaria-sistema
    runtime: php
    plan: free
    region: ohio
    buildCommand: composer install
    startCommand: apache2-foreground
    envVars:
      - key: PHP_VERSION
        value: 8.2