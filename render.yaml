services:
  - type: web
    name: cl-vidracaria-sistema
    env: php
    plan: free
    region: ohio
    buildCommand: composer install
    envVars:
      - key: PHP_VERSION
        value: 8.2